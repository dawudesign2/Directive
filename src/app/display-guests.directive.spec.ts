import { DisplayGuestsDirective } from './display-guests.directive';

describe('DisplayGuestsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayGuestsDirective();
    expect(directive).toBeTruthy();
  });
});
